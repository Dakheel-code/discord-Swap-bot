# دليل الإعداد - بوت توزيع اللاعبين

## 📋 المتطلبات

- Node.js (الإصدار 18 أو أحدث)
- حساب Discord
- حساب Google Cloud
- ملف Google Sheets يحتوي على بيانات اللاعبين

---

## 🔧 خطوات الإعداد التفصيلية

### 1️⃣ تثبيت Node.js

إذا لم يكن لديك Node.js مثبت:
1. اذهب إلى https://nodejs.org/
2. حمّل النسخة LTS
3. قم بتثبيتها
4. تحقق من التثبيت بفتح Terminal وكتابة:
```bash
node --version
npm --version
```

### 2️⃣ إعداد Discord Bot

#### أ. إنشاء التطبيق
1. اذهب إلى https://discord.com/developers/applications
2. اضغط على "New Application"
3. أدخل اسم البوت (مثل: "Player Distribution Bot")
4. اضغط "Create"

#### ب. إنشاء البوت
1. من القائمة الجانبية، اختر "Bot"
2. اضغط "Add Bot" ثم "Yes, do it!"
3. قم بتفعيل الخيارات التالية:
   - ✅ **MESSAGE CONTENT INTENT**
   - ✅ **SERVER MEMBERS INTENT** (اختياري)

#### ج. الحصول على التوكن
1. في صفحة Bot، اضغط "Reset Token"
2. انسخ التوكن (احتفظ به في مكان آمن!)

#### د. دعوة البوت للسيرفر
1. من القائمة الجانبية، اختر "OAuth2" > "URL Generator"
2. في **Scopes**، اختر:
   - ✅ `bot`
   - ✅ `applications.commands`
3. في **Bot Permissions**، اختر:
   - ✅ Send Messages
   - ✅ Send Messages in Threads
   - ✅ Embed Links
   - ✅ Read Message History
   - ✅ Use Slash Commands
4. انسخ الرابط من الأسفل
5. افتح الرابط في المتصفح وادعُ البوت لسيرفرك

#### هـ. الحصول على Guild ID (معرف السيرفر)
1. افتح Discord
2. اذهب إلى Settings > Advanced
3. فعّل "Developer Mode"
4. انقر بزر الماوس الأيمن على سيرفرك
5. اختر "Copy Server ID"

---

### 3️⃣ إعداد Google Sheets API

#### أ. إنشاء مشروع في Google Cloud
1. اذهب إلى https://console.cloud.google.com/
2. اضغط على القائمة المنسدلة للمشاريع في الأعلى
3. اضغط "NEW PROJECT"
4. أدخل اسم المشروع (مثل: "Discord Bot")
5. اضغط "Create"

#### ب. تفعيل Google Sheets API
1. في القائمة الجانبية، اختر "APIs & Services" > "Library"
2. ابحث عن "Google Sheets API"
3. اضغط عليها ثم اضغط "ENABLE"

#### ج. إنشاء Service Account
1. اذهب إلى "APIs & Services" > "Credentials"
2. اضغط "CREATE CREDENTIALS" > "Service Account"
3. أدخل اسم (مثل: "discord-bot-service")
4. اضغط "CREATE AND CONTINUE"
5. في Role، اختر "Editor" (أو يمكنك تركها فارغة)
6. اضغط "DONE"

#### د. إنشاء مفتاح (Key)
1. في صفحة Credentials، اضغط على Service Account الذي أنشأته
2. اذهب إلى تبويب "KEYS"
3. اضغط "ADD KEY" > "Create new key"
4. اختر نوع "JSON"
5. اضغط "CREATE"
6. سيتم تحميل ملف JSON - احفظه في مجلد المشروع باسم `credentials.json`

#### هـ. مشاركة Google Sheet
1. افتح ملف Google Sheets الخاص بك
2. اضغط على زر "Share" في الأعلى
3. انسخ البريد الإلكتروني من ملف `credentials.json` (حقل `client_email`)
4. الصق البريد في خانة المشاركة
5. اختر صلاحية "Viewer" (للقراءة فقط)
6. اضغط "Send"

#### و. الحصول على Sheet ID
من رابط Google Sheet:
```
https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
```
انسخ الجزء الموجود بين `/d/` و `/edit`

---

### 4️⃣ إعداد المشروع

#### أ. تثبيت المكتبات
افتح Terminal في مجلد المشروع واكتب:
```bash
npm install
```

#### ب. إنشاء ملف .env
1. انسخ ملف `.env.example` إلى `.env`:
```bash
copy .env.example .env
```

2. افتح ملف `.env` وأضف بياناتك:
```env
DISCORD_TOKEN=ضع_توكن_البوت_هنا
GUILD_ID=ضع_معرف_السيرفر_هنا
GOOGLE_SHEET_ID=ضع_معرف_الشيت_هنا
GOOGLE_SHEET_RANGE=Sheet1!A:Z
GOOGLE_CREDENTIALS_PATH=./credentials.json
```

#### ج. التحقق من ملف credentials.json
تأكد من أن ملف `credentials.json` موجود في مجلد المشروع

---

### 5️⃣ إعداد Google Sheet

يجب أن يكون الشيت بهذا الشكل:

| Name  | Kills | Deaths | Points | Level |
|-------|-------|--------|--------|-------|
| أحمد  | 150   | 50     | 1500   | 45    |
| سارة  | 120   | 60     | 1200   | 42    |
| محمد  | 180   | 40     | 1800   | 50    |
| ...   | ...   | ...    | ...    | ...   |

**ملاحظات:**
- الصف الأول يجب أن يحتوي على أسماء الأعمدة
- يجب أن يكون هناك عمود للأسماء (Name, Player, USERNAME, إلخ)
- يمكنك إضافة أي أعمدة إضافية للإحصائيات

---

### 6️⃣ تشغيل البوت

```bash
npm start
```

إذا ظهرت هذه الرسائل، فالبوت يعمل بنجاح:
```
✅ Google Sheets API initialized successfully
✅ Bot logged in as YourBot#1234
✅ Registered guild commands
🤖 Bot is ready to receive commands!
```

---

## 📱 استخدام الأوامر

### `/distribute [column_name]`
توزيع اللاعبين بناءً على عمود معين

**مثال:**
```
/distribute Kills
```
سيتم ترتيب اللاعبين حسب عدد القتلات وتوزيعهم على المجموعات الثلاث

---

### `/show`
عرض التوزيع الحالي

---

### `/move [player_name] [group]`
نقل لاعب إلى مجموعة محددة

**مثال:**
```
/move أحمد RGR
```

---

### `/exclude [player_name]`
استثناء لاعب من التوزيع

**مثال:**
```
/exclude أحمد
```

---

### `/include [player_name]`
إعادة لاعب مستثنى

**مثال:**
```
/include أحمد
```

---

### `/refresh`
تحديث البيانات من Google Sheets

---

### `/columns`
عرض الأعمدة المتاحة في الشيت

---

### `/reset`
إعادة تعيين جميع التعديلات اليدوية

---

## 🔍 حل المشاكل الشائعة

### ❌ "Configuration validation failed"
- تأكد من أن ملف `.env` موجود
- تأكد من إضافة `DISCORD_TOKEN` و `GOOGLE_SHEET_ID`

### ❌ "Failed to initialize Google Sheets"
- تأكد من وجود ملف `credentials.json`
- تأكد من مشاركة الشيت مع Service Account Email

### ❌ "No data found in sheet"
- تأكد من أن الشيت يحتوي على بيانات
- تأكد من صحة `GOOGLE_SHEET_RANGE` في ملف `.env`

### ❌ البوت لا يستجيب للأوامر
- تأكد من تفعيل "MESSAGE CONTENT INTENT" في إعدادات البوت
- تأكد من أن البوت لديه الصلاحيات المطلوبة في السيرفر
- انتظر دقيقة واحدة بعد دعوة البوت (الأوامر تحتاج وقت للتسجيل)

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من رسائل الخطأ في Terminal
2. راجع هذا الدليل مرة أخرى
3. تأكد من اتباع جميع الخطوات بالترتيب

---

## 🎯 نصائح

- احتفظ بملف `credentials.json` في مكان آمن ولا تشاركه مع أحد
- لا تشارك `DISCORD_TOKEN` مع أحد
- يمكنك تعديل عدد اللاعبين في كل مجموعة من ملف `src/config.js`
- يمكنك تعديل أسماء المجموعات من نفس الملف
