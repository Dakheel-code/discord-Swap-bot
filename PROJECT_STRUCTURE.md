# هيكل المشروع

```
swap-bot/
│
├── src/                          # المجلد الرئيسي للكود
│   ├── index.js                  # نقطة البداية للتطبيق
│   ├── config.js                 # إعدادات التطبيق والتحقق منها
│   ├── bot.js                    # منطق البوت الرئيسي
│   ├── commands.js               # تعريفات أوامر Discord
│   ├── sheets.js                 # التكامل مع Google Sheets API
│   └── distribution.js           # منطق توزيع اللاعبين
│
├── .env                          # متغيرات البيئة (لا يتم رفعه لـ Git)
├── .env.example                  # مثال لملف البيئة
├── .gitignore                    # ملفات يتم تجاهلها في Git
├── credentials.json              # بيانات اعتماد Google (لا يتم رفعه لـ Git)
├── package.json                  # تبعيات المشروع
├── README.md                     # دليل المشروع (إنجليزي)
├── SETUP_AR.md                   # دليل الإعداد (عربي)
├── EXAMPLES.md                   # أمثلة الاستخدام
└── PROJECT_STRUCTURE.md          # هذا الملف
```

---

## 📁 شرح الملفات

### `src/index.js`
- نقطة البداية للتطبيق
- يتحقق من الإعدادات
- يبدأ Google Sheets API
- يشغل Discord Bot

### `src/config.js`
- يحتوي على جميع الإعدادات
- يقرأ متغيرات البيئة من `.env`
- يتحقق من صحة الإعدادات
- يحدد أسماء المجموعات وعدد اللاعبين

### `src/bot.js`
- يدير اتصال Discord Bot
- يسجل الأوامر (Slash Commands)
- يعالج التفاعلات (Interactions)
- ينفذ منطق كل أمر
- يرسل الرسائل والـ Embeds

### `src/commands.js`
- يعرّف جميع أوامر Discord
- يحدد المعاملات (Parameters) لكل أمر
- يدعم اللغة العربية والإنجليزية

### `src/sheets.js`
- يتصل بـ Google Sheets API
- يقرأ البيانات من الشيت
- يحول الصفوف إلى كائنات JavaScript
- يوفر قائمة الأعمدة المتاحة

### `src/distribution.js`
- يدير منطق توزيع اللاعبين
- يرتب اللاعبين حسب عمود محدد
- يوزع اللاعبين على 3 مجموعات
- يدير النقل اليدوي للاعبين
- يدير استثناء اللاعبين
- ينسق التوزيع للعرض

---

## 🔧 كيف يعمل البوت؟

### 1. التشغيل
```
npm start
  ↓
index.js يبدأ التطبيق
  ↓
config.js يتحقق من الإعدادات
  ↓
sheets.js يتصل بـ Google Sheets
  ↓
bot.js يشغل Discord Bot
  ↓
commands.js يسجل الأوامر
  ↓
✅ البوت جاهز!
```

### 2. معالجة أمر `/distribute`
```
المستخدم يكتب: /distribute Kills
  ↓
bot.js يستقبل الأمر
  ↓
sheets.js يجلب البيانات من Google Sheets
  ↓
distribution.js يرتب اللاعبين حسب Kills
  ↓
distribution.js يوزع اللاعبين على المجموعات
  ↓
bot.js يرسل النتيجة للمستخدم
```

### 3. معالجة أمر `/move`
```
المستخدم يكتب: /move أحمد RGR
  ↓
bot.js يستقبل الأمر
  ↓
distribution.js يبحث عن اللاعب "أحمد"
  ↓
distribution.js يزيل اللاعب من مجموعته الحالية
  ↓
distribution.js يضيف اللاعب إلى RGR
  ↓
distribution.js يحفظ التعديل اليدوي
  ↓
bot.js يرسل تأكيد للمستخدم
```

---

## 🎯 المكونات الرئيسية

### DistributionManager (في distribution.js)
المسؤول عن:
- ✅ تخزين بيانات اللاعبين
- ✅ توزيع اللاعبين على المجموعات
- ✅ إدارة التعديلات اليدوية
- ✅ إدارة الاستثناءات
- ✅ تنسيق البيانات للعرض

### DiscordBot (في bot.js)
المسؤول عن:
- ✅ الاتصال بـ Discord
- ✅ تسجيل الأوامر
- ✅ معالجة التفاعلات
- ✅ إرسال الرسائل
- ✅ تقسيم الرسائل الطويلة

### Google Sheets Integration (في sheets.js)
المسؤول عن:
- ✅ المصادقة مع Google API
- ✅ قراءة البيانات
- ✅ تحويل البيانات إلى تنسيق قابل للاستخدام

---

## 🔐 الأمان

### ملفات لا يجب مشاركتها:
- ❌ `.env` - يحتوي على التوكنات
- ❌ `credentials.json` - يحتوي على مفاتيح Google
- ❌ `node_modules/` - مكتبات خارجية

### ملفات آمنة للمشاركة:
- ✅ جميع ملفات `src/`
- ✅ `package.json`
- ✅ `.env.example`
- ✅ ملفات التوثيق (README, SETUP, إلخ)

---

## 🚀 التطوير المستقبلي

### ميزات يمكن إضافتها:
1. **حفظ التوزيعات:**
   - حفظ التوزيعات في قاعدة بيانات
   - استرجاع توزيعات سابقة

2. **إحصائيات متقدمة:**
   - عرض إحصائيات كل مجموعة
   - مقارنة المجموعات

3. **توزيع ذكي:**
   - توزيع متوازن بناءً على عدة أعمدة
   - خوارزميات توزيع مختلفة

4. **تصدير البيانات:**
   - تصدير التوزيع إلى ملف Excel
   - تصدير إلى صورة

5. **إشعارات:**
   - إشعار اللاعبين بمجموعاتهم
   - إرسال رسائل خاصة

6. **واجهة ويب:**
   - لوحة تحكم لإدارة التوزيعات
   - عرض مرئي للمجموعات

---

## 📊 تدفق البيانات

```
Google Sheets
     ↓
[sheets.js] يقرأ البيانات
     ↓
Array of Players
     ↓
[distribution.js] يعالج ويوزع
     ↓
Groups Object {RGR: [], OTL: [], RND: []}
     ↓
[bot.js] ينسق ويعرض
     ↓
Discord Message
```

---

## 🛠️ التخصيص

### تغيير عدد اللاعبين في كل مجموعة
في `src/config.js`:
```javascript
groups: {
  names: ['RGR', 'OTL', 'RND'],
  maxPlayersPerGroup: 50,  // غيّر هذا الرقم
}
```

### تغيير أسماء المجموعات
في `src/config.js`:
```javascript
groups: {
  names: ['Team A', 'Team B', 'Team C'],  // غيّر الأسماء
  maxPlayersPerGroup: 50,
}
```

### إضافة مجموعة رابعة
في `src/config.js`:
```javascript
groups: {
  names: ['RGR', 'OTL', 'RND', 'NEW'],  // أضف مجموعة جديدة
  maxPlayersPerGroup: 50,
}
```

وفي `src/commands.js`، أضف الخيار الجديد:
```javascript
.addChoices(
  { name: 'RGR', value: 'RGR' },
  { name: 'OTL', value: 'OTL' },
  { name: 'RND', value: 'RND' },
  { name: 'NEW', value: 'NEW' }  // أضف هذا
)
```
