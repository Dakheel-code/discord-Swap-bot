# دليل استخدام أوامر Move و Hold

## نظرة عامة

تم تحديث أوامر `/move` و `/hold` لتعمل مع صفحة **Master_CSV** عمود **E (Action)** بدلاً من صفحة DiscordMap.

---

## 📋 متطلبات الإعداد

### 1. هيكل صفحة Master_CSV

يجب أن تحتوي صفحة `Master_CSV` على الأعمدة التالية:

```
A: Player_ID     - معرف اللاعب في اللعبة
B: Discord-ID    - معرف Discord الرقمي (مثل: 123456789012345678)
C: Name          - اسم اللاعب
D: Trophies      - نقاط اللاعب
E: Action        - عمود الإجراءات (هنا يكتب البوت)
```

### 2. ملف .env

تأكد من أن ملف `.env` يحتوي على:

```env
GOOGLE_SHEET_RANGE=Master_CSV!A:Z
GOOGLE_SERVICE_ACCOUNT_PATH=./credentials.json
```

---

## 🎮 الأوامر

### 1️⃣ أمر `/move` - نقل لاعب

**الوظيفة:** نقل لاعب إلى كلان محدد

**الاستخدام:**
```
/move player:@Username clan:RGR
```

**ماذا يحدث:**
1. البوت يبحث عن Discord ID للاعب في صفحة Master_CSV
2. يكتب اسم الكلان في عمود E (Action)
3. يحدث رسالة التوزيع تلقائياً

**مثال:**
```
/move player:@Ahmed clan:RGR
```
النتيجة في عمود E: `RGR`

---

### 2️⃣ أمر `/hold` - استثناء لاعب

**الوظيفة:** استثناء لاعب من التوزيع التلقائي

**الاستخدام:**
```
/hold player:@Username
```

**ماذا يحدث:**
1. البوت يبحث عن Discord ID للاعب في صفحة Master_CSV
2. يكتب "Hold" في عمود E (Action)
3. اللاعب يظهر في قسم WILDCARDS مع ملاحظة "stays in clan"

**مثال:**
```
/hold player:@Sara
```
النتيجة في عمود E: `Hold`

---

### 3️⃣ أمر `/include` - إعادة لاعب

**الوظيفة:** إعادة لاعب مستثنى إلى التوزيع

**الاستخدام:**
```
/include player:@Username
```

**ماذا يحدث:**
1. البوت يبحث عن Discord ID للاعب في صفحة Master_CSV
2. يمسح محتوى عمود E (Action)
3. اللاعب يعود للتوزيع التلقائي

**مثال:**
```
/include player:@Sara
```
النتيجة في عمود E: `(فارغ)`

---

## 📊 أمثلة عملية

### مثال 1: نقل 3 لاعبين إلى RGR

```
/move player:@Ahmed clan:RGR
/move player:@Sara clan:RGR
/move player:@Ali clan:RGR
```

**النتيجة في Master_CSV:**
| Discord-ID | Name  | Action |
|------------|-------|--------|
| 123...     | Ahmed | RGR    |
| 456...     | Sara  | RGR    |
| 789...     | Ali   | RGR    |

---

### مثال 2: استثناء لاعبين

```
/hold player:@Khaled
/hold player:@Fatima
```

**النتيجة في Master_CSV:**
| Discord-ID | Name   | Action |
|------------|--------|--------|
| 111...     | Khaled | Hold   |
| 222...     | Fatima | Hold   |

---

### مثال 3: إعادة لاعب للتوزيع

```
/include player:@Khaled
```

**النتيجة في Master_CSV:**
| Discord-ID | Name   | Action |
|------------|--------|--------|
| 111...     | Khaled |        |
| 222...     | Fatima | Hold   |

---

## 🔍 كيفية ربط اللاعبين

قبل استخدام `/move` أو `/hold`، يجب أن يكون اللاعب مربوطاً بحساب Discord:

### استخدام أمر `/map`

```
/map ingame_id:PlayerName123 discord_id:@Username
```

**مثال:**
```
/map ingame_id:Ahmed123 discord_id:@Ahmed
```

هذا يربط اسم اللاعب في اللعبة بحساب Discord.

---

## ✅ رسائل النجاح

### عند نجاح `/move`:
```
✅ Players Moved
Moved to RGR:
• Ahmed

✅ Updated in Master_CSV sheet (Column E: Action)
✅ Distribution message updated
```

### عند نجاح `/hold`:
```
✅ Players Excluded
Excluded:
• Sara

✅ Updated in Master_CSV sheet (Column E: Action = "Hold")
✅ Distribution message updated
```

### عند نجاح `/include`:
```
✅ Player Included
Khaled has been added back to distribution

✅ Cleared Column E in Master_CSV sheet
✅ Distribution message updated
```

---

## ❌ رسائل الخطأ الشائعة

### 1. "No data found in Master_CSV sheet"
**السبب:** الصفحة غير موجودة أو الاسم خاطئ  
**الحل:** 
- تأكد من أن اسم الصفحة هو `Master_CSV` بالضبط
- تحقق من `GOOGLE_SHEET_RANGE` في ملف `.env`

### 2. "Discord-ID column not found"
**السبب:** لا يوجد عمود Discord-ID في الصفحة  
**الحل:**
- أضف عمود باسم "Discord-ID" في صفحة Master_CSV
- تأكد من أن الاسم صحيح (يمكن أن يكون Discord-ID أو Discord_ID)

### 3. "Player with Discord ID not found"
**السبب:** اللاعب غير موجود في صفحة Master_CSV  
**الحل:**
- استخدم أمر `/map` لربط اللاعب أولاً
- تحقق من أن Discord ID موجود في عمود Discord-ID

### 4. "Write access not available"
**السبب:** Service Account غير مُعد بشكل صحيح  
**الحل:**
- تأكد من وجود ملف `credentials.json`
- تحقق من أن Service Account لديه صلاحيات الكتابة
- راجع `GOOGLE_SERVICE_ACCOUNT_PATH` في `.env`

---

## 🔧 استكشاف الأخطاء

### التحقق من الإعدادات

1. **تحقق من ملف .env:**
```env
GOOGLE_SHEET_RANGE=Master_CSV!A:Z
GOOGLE_SERVICE_ACCOUNT_PATH=./credentials.json
```

2. **تحقق من صفحة Master_CSV:**
- افتح Google Sheet
- تأكد من وجود صفحة باسم "Master_CSV"
- تحقق من وجود عمود "Discord-ID"
- تحقق من وجود عمود "Action" (عمود E)

3. **تحقق من Service Account:**
- تأكد من وجود ملف `credentials.json`
- تأكد من مشاركة الـ Sheet مع Service Account email
- تحقق من صلاحيات الكتابة

### سجلات التشخيص

عند تشغيل الأوامر، سترى في Console:

```
🔍 Searching for Discord ID: "123456789"
📊 Sheet: Master_CSV
📊 Total rows: 150
📋 Headers: Player_ID | Discord-ID | Name | Trophies | Action
✅ Found Discord-ID column at index 1
✅ Found player at row 42
✅ Updated Action for Discord ID 123456789 to "RGR" at Master_CSV!E42
```

---

## 📝 ملاحظات مهمة

1. **عمود E فقط:** الأوامر تكتب فقط في عمود E (Action)
2. **البحث بـ Discord ID:** البوت يبحث عن اللاعبين باستخدام Discord ID وليس الاسم
3. **التحديث التلقائي:** رسائل التوزيع تُحدث تلقائياً بعد كل أمر
4. **الأولوية:** اللاعبين في WILDCARDS لهم أولوية على التوزيع التلقائي

---

## 🎯 سير العمل الموصى به

### للتوزيع الموسمي:

1. **التحضير:**
```
/refresh                    # تحديث البيانات من Google Sheets
```

2. **استثناء اللاعبين:**
```
/hold player:@Player1       # لاعبين لن ينتقلوا
/hold player:@Player2
```

3. **النقل اليدوي:**
```
/move player:@Leader1 clan:RGR    # قادة الكلانات
/move player:@Leader2 clan:OTL
```

4. **التوزيع التلقائي:**
```
/swap season:157            # توزيع باقي اللاعبين
```

5. **الجدولة:**
```
/schedule datetime:2024-12-25 14:30 channel:#announcements
```

---

## 🆘 الدعم

إذا واجهت مشاكل:

1. تحقق من سجلات Console للأخطاء
2. تأكد من الإعدادات في `.env`
3. راجع هيكل صفحة Master_CSV
4. تحقق من صلاحيات Service Account

---

**آخر تحديث:** 7 ديسمبر 2025
