# 📖 كيفية استخدام البوت

## ⚠️ مهم جداً!

عند استخدام أوامر `/move` و `/hold` و `/include`، يجب أن يكون اللاعب **مسجلاً مسبقاً** في صفحة `DiscordMap`.

---

## 🔄 الخطوات الصحيحة:

### 1️⃣ أولاً: تسجيل اللاعب باستخدام `/map`

```
/map ingame_id:26935576 discord_id:@Dakheel
```

هذا سيضيف اللاعب في صفحة `DiscordMap`:

| A (Ingame-ID) | B (Discord-ID) | C (Action) |
|---------------|----------------|------------|
| 26935576      | 767810845035593748 | |

**ملاحظة**: عمود B سيحتوي على Discord ID الرقمي (مثل: `767810845035593748`)

---

### 2️⃣ ثانياً: نقل اللاعب باستخدام `/move`

```
/move players:@Dakheel clan:RGR
```

البوت سيقوم بـ:
1. استخراج Discord ID من `@Dakheel`
2. البحث عنه في عمود B في `DiscordMap`
3. كتابة "RGR" في عمود C

النتيجة:

| A (Ingame-ID) | B (Discord-ID) | C (Action) |
|---------------|----------------|------------|
| 26935576      | 767810845035593748 | **RGR** |

---

## 🎯 الأوامر المتاحة:

### `/map` - ربط حساب Discord بمعرف اللعبة
```
/map ingame_id:26935576 discord_id:@Dakheel
```

### `/move` - نقل لاعب إلى كلان
```
/move players:@Dakheel clan:RGR
```
- يكتب اسم الكلان في عمود C في `DiscordMap`

### `/hold` - استبعاد لاعب
```
/hold players:@Dakheel
```
- يكتب "Hold" في عمود C في `DiscordMap`

### `/include` - إعادة إدراج لاعب
```
/include player:@Dakheel
```
- يمسح محتوى عمود C في `DiscordMap`

---

## 📊 بنية صفحة DiscordMap:

```
| A (Ingame-ID) | B (Discord-ID)     | C (Action) |
|---------------|--------------------|------------|
| 26935576      | 767810845035593748 |            |
| 29265908      | 570806567004405761 | Hold       |
| 28605615      | 838414458392543282 | RGR        |
```

- **عمود A**: معرف اللاعب في اللعبة (Ingame-ID)
- **عمود B**: معرف Discord الرقمي (Discord-ID)
- **عمود C**: الإجراء (Action) - اسم الكلان أو "Hold" أو فارغ

---

## ❓ حل المشاكل:

### مشكلة: "Player not found"

**السبب**: اللاعب غير مسجل في `DiscordMap`

**الحل**: استخدم `/map` أولاً لتسجيل اللاعب

### مشكلة: "Discord ID not found in DiscordMap sheet"

**السبب**: Discord ID غير موجود في عمود B

**الحل**: 
1. تأكد من استخدام `/map` لإضافة اللاعب
2. تحقق من أن عمود B يحتوي على Discord ID الصحيح

---

## 🧪 اختبار:

### 1. تسجيل لاعب:
```
/map ingame_id:TestPlayer123 discord_id:@YourName
```

### 2. نقل اللاعب:
```
/move players:@YourName clan:RGR
```

### 3. التحقق:
- افتح صفحة `DiscordMap`
- ابحث عن Discord ID الخاص بك في عمود B
- تأكد أن "RGR" مكتوب في عمود C
