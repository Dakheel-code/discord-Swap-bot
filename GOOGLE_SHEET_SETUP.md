# 📊 إعداد Google Sheet

## ✅ المطلوب:

### صفحة 1: DiscordMap

| العمود A | العمود B | العمود C |
|----------|----------|----------|
| Player_ID | Discord_ID | ID (Ingame-ID) |
| 1 | 123456789 | Player123 |
| 2 | 987654321 | Player456 |

**ملاحظات:**
- العمود B: Discord_ID (الرقم فقط، بدون `<@>`)
- العمود C: اسمه يجب أن يكون `ID` أو `Ingame-ID`

---

### صفحة 2: Master_CSV

يجب أن يحتوي على:
- **العمود C**: اسمه `ID` أو `Ingame-ID` أو `Player_ID`
- **عمود Action**: اسمه `Action`

مثال:

| A (Name) | B (Trophies) | C (ID) | D (Clan) | E (Action) |
|----------|--------------|--------|----------|------------|
| Ahmed | 5000 | Player123 | RGR | |
| Sara | 4500 | Player456 | OTL | Hold |

---

## 🔄 كيف يعمل البوت:

### عند استخدام `/move players:@Dakheel clan:RGR`:

1. **الخطوة 1**: البحث في DiscordMap
   - يستخرج Discord_ID من `@Dakheel`
   - يبحث في عمود B (Discord_ID)
   - يحصل على Ingame-ID من عمود C

2. **الخطوة 2**: الكتابة في Master_CSV
   - يبحث عن Ingame-ID في عمود C
   - يكتب "RGR" في عمود Action

---

## ⚠️ تأكد من:

1. ✅ صفحة `DiscordMap` موجودة
2. ✅ صفحة `Master_CSV` موجودة
3. ✅ عمود C في Master_CSV اسمه `ID` أو `Ingame-ID`
4. ✅ عمود Action موجود في Master_CSV
5. ✅ تم ربط اللاعبين في DiscordMap باستخدام `/map`

---

## 🧪 اختبار:

### 1. ربط لاعب:
```
/map ingame_id:Player123 discord_id:@Dakheel
```

### 2. نقل اللاعب:
```
/move players:@Dakheel clan:RGR
```

### 3. التحقق:
- افتح Master_CSV
- ابحث عن Player123 في عمود C
- تأكد أن "RGR" مكتوب في عمود Action
