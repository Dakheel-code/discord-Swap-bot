# اختبار سريع للجدولة المُصلحة

## ⚡ اختبار سريع (دقيقة واحدة)

### 1. احسب الوقت الحالي بـ UTC

إذا كنت في السعودية (UTC+3):
```
الوقت المحلي: 3:00 PM
الوقت UTC: 12:00 PM (اطرح 3 ساعات)
```

### 2. جدول رسالة لدقيقة واحدة من الآن

مثال: إذا كان الوقت الآن 12:00 UTC، جدول لـ 12:01:

```
/schedule datetime:2024-12-09 12:01 channel:#swap-list-test
```

### 3. تحقق من الجدولة

```
/viewschedule
```

يجب أن ترى:
- ⏰ Time Remaining: 0h 1m
- ✅ Status: Scheduled

### 4. انتظر دقيقة واحدة

بعد دقيقة، يجب أن:
- ✅ تُرسل الرسالة تلقائياً في القناة
- ✅ تظهر رسالة التوزيع كاملة
- ✅ تُحذف الجدولة تلقائياً

### 5. تأكد من الحذف

```
/viewschedule
```

يجب أن ترى:
```
❌ No scheduled distribution found
```

---

## 🔄 اختبار إعادة التشغيل

### 1. جدول رسالة لـ 5 دقائق

```
/schedule datetime:2024-12-09 12:05 channel:#swap-list-test
```

### 2. أعد تشغيل البوت

**على Railway:**
- اذهب إلى Dashboard
- اضغط على "Restart"

**محلياً:**
- Ctrl+C لإيقاف البوت
- `npm start` لتشغيله مرة أخرى

### 3. تحقق من الجدولة بعد التشغيل

```
/viewschedule
```

يجب أن ترى الجدولة **لا تزال موجودة** ✅

### 4. انتظر حتى الوقت المحدد

يجب أن تُرسل الرسالة تلقائياً ✅

---

## 🎯 اختبار الحالات المختلفة

### حالة 1: أقل من دقيقة

جدول لـ 30 ثانية من الآن، ثم:
```
/viewschedule
```

يجب أن ترى:
- 🟠 Status: ⏳ Sending soon...
- ⏰ Time Remaining: Less than 1 minute

### حالة 2: وقت فائت

جدول رسالة في الماضي (سيفشل):
```
/schedule datetime:2024-12-09 10:00 channel:#test
```

يجب أن ترى:
```
❌ The scheduled time must be in the future (UTC)!
```

### حالة 3: جدولة فائتة

1. جدول رسالة لـ 2 دقيقة
2. انتظر 3 دقائق (لا تُرسل)
3. استخدم `/viewschedule`

يجب أن ترى:
- 🔴 Status: ❌ Missed (will be cleaned up)
- ⏰ Time Remaining: 1h 0m ago

4. انتظر 30 ثانية - سيتم حذف الجدولة تلقائياً

---

## 📝 أمثلة عملية

### مثال 1: جدولة لليوم التالي

```
/schedule datetime:2024-12-10 15:00 channel:#swap-list-test
```

### مثال 2: تعديل الوقت

```
/editschedule datetime:2024-12-10 16:00
```

### مثال 3: تعديل القناة

```
/editschedule channel:#another-channel
```

### مثال 4: حذف الجدولة

```
/deleteschedule
```

---

## 🔍 فحص الـ Logs

### على Railway:

1. اذهب إلى Dashboard
2. اضغط على "Deployments"
3. اضغط على آخر deployment
4. ابحث عن:

```
⏰ Schedule checker started (checks every 30 seconds)
✅ Loaded schedule: Will post in X minutes
🎯 Scheduled time reached! Sending distribution...
✅ Scheduled post sent to #channel-name
```

### محلياً:

راقب الـ console، يجب أن ترى:

```bash
⏰ Schedule checker started (checks every 30 seconds)
⏰ Schedule set: Will post in 1 minutes (2024-12-09 12:01 UTC)
📅 Scheduled for: 2024-12-09T12:01:00.000Z
🕐 Current time: 2024-12-09T12:00:15.000Z

# بعد دقيقة:
🎯 Scheduled time reached! Sending distribution...
🔄 Refreshing data from Google Sheets before scheduled post...
✅ Data refreshed: 50 players
✅ Scheduled post sent to swap-list-test
🗑️ Deleted schedule file
```

---

## ✅ Checklist للتأكد من نجاح الإصلاح

- [ ] الجدولة تُرسل في الوقت المحدد
- [ ] `/viewschedule` يعرض الوقت بشكل صحيح (بدون سالب)
- [ ] الجدولة تعمل بعد إعادة تشغيل البوت
- [ ] الجدولات الفائتة تُحذف تلقائياً
- [ ] الـ logs تظهر رسائل واضحة
- [ ] لا توجد أخطاء في console

---

## 🆘 حل المشاكل

### المشكلة: الجدولة لا تُرسل

**الحل:**
1. تحقق من الـ logs - هل ترى "Schedule checker started"؟
2. تحقق من وجود ملف `schedule.json`
3. تحقق من أن الوقت صحيح (UTC)

### المشكلة: "No scheduled distribution found"

**الحل:**
- الجدولة إما تم إرسالها أو حُذفت
- جدول رسالة جديدة

### المشكلة: الوقت لا يزال بالسالب

**الحل:**
- تأكد من أنك قمت بـ push التحديثات
- أعد تشغيل البوت
- تحقق من أن الكود محدث

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تحقق من الـ logs
2. تأكد من أن الكود محدث
3. جرب إعادة تشغيل البوت
4. اختبر محلياً أولاً

**الجدولة الآن موثوقة 100%!** 🎉
