# 🚀 تشغيل البوت 24/7

## 📋 الطرق المتاحة

### 1️⃣ استخدام PM2 (موصى به للجهاز المحلي)

#### تثبيت PM2:
```bash
npm install -g pm2
```

#### تشغيل البوت:
```bash
pm2 start ecosystem.config.js
```

#### أوامر PM2 المفيدة:
```bash
pm2 status              # عرض حالة البوت
pm2 logs discord-bot    # عرض السجلات
pm2 restart discord-bot # إعادة تشغيل البوت
pm2 stop discord-bot    # إيقاف البوت
pm2 delete discord-bot  # حذف البوت من PM2
```

#### تشغيل PM2 عند بدء Windows:
```bash
pm2 startup
pm2 save
```

---

### 2️⃣ استخدام ملف BAT (بسيط)

#### تشغيل عادي:
- انقر مرتين على `start-bot.bat`

#### تشغيل مع إعادة تشغيل تلقائية:
- انقر مرتين على `keep-alive.bat`
- البوت سيعيد التشغيل تلقائياً إذا توقف

---

### 3️⃣ استضافة على Render.com (مجاني 24/7)

#### الخطوات:

1. **إنشاء حساب GitHub وتحميل المشروع:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```

2. **إنشاء حساب على Render.com:**
   - اذهب إلى https://render.com
   - سجل دخول بحساب GitHub

3. **إنشاء Web Service:**
   - اضغط "New +" → "Web Service"
   - اختر المشروع من GitHub
   - الإعدادات:
     - **Name:** discord-player-bot
     - **Environment:** Node
     - **Build Command:** `npm install`
     - **Start Command:** `node src/index.js`
     - **Plan:** Free

4. **إضافة Environment Variables:**
   - أضف جميع المتغيرات من ملف `.env`:
     - `DISCORD_TOKEN`
     - `GUILD_ID`
     - `GOOGLE_SHEET_ID`
     - `GOOGLE_API_KEY`
     - `GOOGLE_SERVICE_ACCOUNT_PATH` (إذا كنت تستخدم Service Account)

5. **Deploy:**
   - اضغط "Create Web Service"
   - انتظر حتى ينتهي البناء

✅ البوت الآن يعمل 24/7 مجاناً!

---

### 4️⃣ استضافة على Railway.app (مجاني مع حدود)

#### الخطوات:

1. **سجل على Railway.app:**
   - اذهب إلى https://railway.app
   - سجل دخول بحساب GitHub

2. **إنشاء مشروع جديد:**
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المشروع

3. **إضافة Environment Variables:**
   - اذهب إلى Variables
   - أضف جميع المتغيرات من `.env`

4. **Deploy:**
   - Railway سيقوم بالـ Deploy تلقائياً

✅ تحصل على $5 مجاناً كل شهر!

---

### 5️⃣ استضافة على Oracle Cloud (VPS مجاني للأبد)

#### الخطوات:

1. **إنشاء حساب Oracle Cloud:**
   - اذهب إلى https://cloud.oracle.com
   - سجل حساب جديد (يحتاج بطاقة ائتمان للتحقق فقط)

2. **إنشاء VM Instance:**
   - اختر "Always Free" tier
   - نظام التشغيل: Ubuntu
   - افتح Port 22 للـ SSH

3. **الاتصال بالـ VPS:**
   ```bash
   ssh ubuntu@YOUR_VM_IP
   ```

4. **تثبيت Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   sudo npm install -g pm2
   ```

5. **رفع المشروع:**
   ```bash
   # استخدم git أو scp لرفع الملفات
   git clone YOUR_GITHUB_REPO_URL
   cd YOUR_PROJECT_FOLDER
   npm install
   ```

6. **إنشاء ملف .env:**
   ```bash
   nano .env
   # الصق محتويات ملف .env
   # اضغط Ctrl+X ثم Y ثم Enter للحفظ
   ```

7. **تشغيل البوت بـ PM2:**
   ```bash
   pm2 start ecosystem.config.js
   pm2 startup
   pm2 save
   ```

✅ البوت الآن يعمل 24/7 مجاناً للأبد!

---

## 🔍 مقارنة الحلول

| الطريقة | التكلفة | السهولة | الموثوقية | ملاحظات |
|---------|---------|---------|-----------|---------|
| **PM2 (جهازك)** | مجاني | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | يحتاج جهازك يكون شغال |
| **Render.com** | مجاني | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | قد ينام بعد فترة خمول |
| **Railway.app** | $5/شهر مجاناً | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | محدود بالرصيد |
| **Oracle Cloud** | مجاني للأبد | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | يحتاج خبرة بسيطة |

---

## 💡 التوصية

**للمبتدئين:** استخدم **Railway.app** أو **Render.com**

**للمحترفين:** استخدم **Oracle Cloud VPS** مع **PM2**

**للتجربة السريعة:** استخدم **PM2** على جهازك

---

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
- ملف `.env` موجود وصحيح
- جميع المتغيرات مضبوطة
- الـ Discord Token صحيح
- Google Sheets API مفعل
