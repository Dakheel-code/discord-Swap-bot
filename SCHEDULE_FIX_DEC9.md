# إصلاح مشكلة الجدولة - 9 ديسمبر 2024

## المشكلة الأصلية

عند جدولة رسالة باستخدام `/schedule`، كانت المشاكل التالية تحدث:

1. **الجدولة لا تُنفذ في الوقت المحدد** - الرسالة لا تُرسل عندما يحين الوقت
2. **الوقت المتبقي يصبح بالسالب** - بعد مرور الوقت، يظهر `-7h -20m` بدلاً من رسالة خطأ واضحة
3. **الاعتماد الكامل على `setTimeout`** - والذي له مشاكل:
   - حد أقصى للتأخير (24.8 يوم)
   - يُفقد عند إعادة تشغيل البوت
   - غير موثوق للجدولة طويلة المدى

## الحل المطبق

### 1. نظام فحص دوري (Schedule Checker)

تم إضافة نظام يفحص كل **30 ثانية** إذا حان وقت إرسال الرسالة المجدولة:

```javascript
// في constructor
this.scheduleCheckInterval = null;

// في onReady()
this.startScheduleChecker();

// الدالة الجديدة
startScheduleChecker() {
  this.scheduleCheckInterval = setInterval(async () => {
    await this.checkAndExecuteSchedule();
  }, 30000); // كل 30 ثانية
}
```

### 2. دالة فحص وتنفيذ الجدولة

```javascript
async checkAndExecuteSchedule() {
  // تحميل الجدولة من الملف إذا لم تكن في الذاكرة
  if (!this.scheduledData && fs.existsSync(this.scheduleFilePath)) {
    this.scheduledData = JSON.parse(fs.readFileSync(this.scheduleFilePath, 'utf8'));
  }

  // حساب الفرق الزمني
  const timeDiff = scheduledDate.getTime() - now.getTime();
  
  // تنفيذ إذا حان الوقت (نافذة دقيقة واحدة)
  if (timeDiff <= 0 && timeDiff > -60000) {
    await this.executeScheduledPost();
  }
  // حذف إذا تأخر أكثر من دقيقة
  else if (timeDiff <= -60000) {
    this.deleteSchedule();
  }
}
```

### 3. دالة تنفيذ منفصلة

تم فصل كود التنفيذ في دالة مستقلة `executeScheduledPost()`:

```javascript
async executeScheduledPost() {
  // جلب القناة
  const channel = await this.client.channels.fetch(this.scheduledData.channelId);
  
  // تحديث البيانات من Google Sheets
  this.playersData = await fetchPlayersDataWithDiscordNames();
  this.distributionManager.distribute(this.playersData, 'Trophies');
  
  // إرسال الرسالة
  const formattedText = this.distributionManager.getFormattedDistribution();
  await this.sendLongMessage(channel, formattedText, true, true);
  
  // تنظيف
  this.deleteSchedule();
}
```

### 4. تحسين عرض الوقت المتبقي

في `/viewschedule`، تم إضافة معالجة للحالات المختلفة:

```javascript
if (timeRemaining > 60000) {
  // أكثر من دقيقة في المستقبل
  status = '✅ Scheduled';
  color = Green;
} else if (timeRemaining > 0) {
  // أقل من دقيقة
  status = '⏳ Sending soon...';
  color = Orange;
} else if (timeRemaining > -60000) {
  // متأخر أقل من دقيقة (في نافذة التنفيذ)
  status = '⏳ Executing...';
  color = Orange;
} else {
  // متأخر أكثر من دقيقة
  status = '❌ Missed (will be cleaned up)';
  color = Red;
  timeText = '7h 20m ago'; // بدلاً من -7h -20m
}
```

### 5. تبسيط handleSchedule و handleEditSchedule

- إزالة كود `setTimeout` المعقد
- الاعتماد على Schedule Checker فقط
- إضافة logging مفصل للتتبع

## الميزات الجديدة

### ✅ موثوقية عالية
- الفحص كل 30 ثانية يضمن عدم تفويت الجدولة
- يعمل حتى بعد إعادة تشغيل البوت
- لا يوجد حد أقصى للوقت

### ✅ معالجة أخطاء محسّنة
- رسائل واضحة للحالات المختلفة
- تنظيف تلقائي للجدولات الفائتة
- logging مفصل لتتبع المشاكل

### ✅ عرض دقيق للحالة
- ألوان مختلفة حسب الحالة (أخضر/برتقالي/أحمر)
- رسائل واضحة (Scheduled/Sending soon/Executing/Missed)
- لا مزيد من الأرقام السالبة

## الملفات المعدلة

### `src/bot.js`

#### إضافات جديدة:
- `this.scheduleCheckInterval` - متغير لحفظ الـ interval
- `startScheduleChecker()` - بدء نظام الفحص الدوري
- `checkAndExecuteSchedule()` - فحص وتنفيذ الجدولة
- `executeScheduledPost()` - تنفيذ إرسال الرسالة المجدولة

#### تحديثات:
- `onReady()` - إضافة `this.startScheduleChecker()`
- `loadSchedule()` - تبسيط (إزالة setTimeout)
- `handleSchedule()` - تبسيط وتحسين الرسائل
- `handleViewSchedule()` - إضافة معالجة للوقت السالب
- `handleEditSchedule()` - تبسيط (إزالة setTimeout)

## الاستخدام

### جدولة رسالة
```
/schedule datetime:2024-12-09 15:00 channel:#swap-list-test
```

### عرض الجدولة الحالية
```
/viewschedule
```

### تعديل الجدولة
```
/editschedule datetime:2024-12-09 16:00
/editschedule channel:#another-channel
```

### حذف الجدولة
```
/deleteschedule
```

## كيف يعمل النظام

```
┌─────────────────────────────────────────────────────────┐
│                    Bot Starts                           │
│                         ↓                               │
│              startScheduleChecker()                     │
│                         ↓                               │
│         ┌───────────────────────────┐                   │
│         │  Every 30 seconds:        │                   │
│         │  checkAndExecuteSchedule()│                   │
│         └───────────────┬───────────┘                   │
│                         ↓                               │
│              Load schedule.json                         │
│                         ↓                               │
│           Calculate time difference                     │
│                         ↓                               │
│         ┌───────────────┴───────────────┐               │
│         │                               │               │
│    Time > 1min      -1min < Time < 0    Time < -1min   │
│      future              now              past          │
│         │                               │               │
│      Wait          executeScheduledPost() Clean up      │
│                            ↓                            │
│                    Send distribution                    │
│                            ↓                            │
│                    Delete schedule.json                 │
└─────────────────────────────────────────────────────────┘
```

## الفوائد

1. **لا مزيد من الجدولات الفائتة** - الفحص الدوري يضمن التنفيذ
2. **يعمل بعد إعادة التشغيل** - يقرأ من schedule.json تلقائياً
3. **واجهة واضحة** - رسائل وألوان تعبر عن الحالة بدقة
4. **سهل الصيانة** - كود أبسط وأقل تعقيداً
5. **موثوق على Railway** - لا يتأثر بإعادة تشغيل الخدمة

## الاختبار

### اختبار جدولة قصيرة (دقيقة واحدة)
```
/schedule datetime:2024-12-09 13:01 channel:#test
```
انتظر دقيقة وتحقق من إرسال الرسالة.

### اختبار إعادة التشغيل
1. جدول رسالة لـ 5 دقائق قادمة
2. أعد تشغيل البوت
3. تحقق من أن الجدولة لا تزال موجودة (`/viewschedule`)
4. انتظر حتى الوقت المحدد

### اختبار الوقت الفائت
1. جدول رسالة في الماضي (سيفشل)
2. أو انتظر حتى يمر الوقت بأكثر من دقيقة
3. استخدم `/viewschedule` - يجب أن ترى "Missed"
4. انتظر 30 ثانية - سيتم حذف الجدولة تلقائياً

## ملاحظات مهمة

- ⏰ **التوقيت بـ UTC** - تأكد من إدخال الوقت بتوقيت UTC
- 🔄 **الفحص كل 30 ثانية** - قد يكون هناك تأخير حتى 30 ثانية
- 📁 **schedule.json** - لا تحذف هذا الملف يدوياً أثناء وجود جدولة
- 🔁 **جدولة واحدة فقط** - الجدولة الجديدة تستبدل القديمة

## التحديثات المستقبلية المقترحة

1. دعم جدولات متعددة
2. جدولة متكررة (يومية/أسبوعية)
3. إشعارات قبل الإرسال (5 دقائق قبل)
4. واجهة لعرض جميع الجدولات
